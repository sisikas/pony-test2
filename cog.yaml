buildras.
  gpu: true
  cuda: "11.8"
  python_version: "3.10"
  python_packages:
    - "torch>=2.0.0"
    - "diffusers>=0.21.0"
    - "transformers>=4.30.0"
    - "accelerate>=0.20.0"
    - "safetensors>=0.3.0"
    - "pillow>=9.0.0"
    - "numpy>=1.24.0"
    - "requests>=2.28.0"
    - "peft>=0.4.0"
